Update the RMCW admin page with the following changes:
1. GRANULAR FLAGGING: Each submission card has two feedback blocks — feedback_text and guidance_text. Add a separate Flag button for each block individually. Flagging feedback_text sets feedback_flagged=true and saves a feedback_flag_reason. Flagging guidance_text sets guidance_flagged=true and saves a guidance_flag_reason. A flagged individual block shows a yellow highlight and the flag reason on that block only. The other block on the same card is unaffected.
2. ARCHIVE: Add an Archive button to each submission card alongside Delete and Flag. Archiving sets archived=true on the submission. Archived submissions disappear from the main ALL SUBMISSIONS feed and move to a collapsed ARCHIVED section at the bottom of the admin page. An Unarchive button on archived cards restores them to the main feed. Archived submissions still appear on the College Showcase page — archive is for admin organisation only, not public visibility. Add /api/admin/archive/:id and /api/admin/unarchive/:id endpoints.
3. DELETE BEHAVIOR: Delete sets deleted=true (soft delete as currently implemented). Deleted submissions are hidden from the College Showcase and from the admin main feed. Add a DELETED section at the very bottom of the admin page showing soft-deleted submissions with a Restore button that sets deleted=false. This allows recovery of accidentally deleted submissions.
4. ADMIN SECURITY: Replace the current ?password= URL authentication entirely. Create a login page at /admin that shows a simple centered password form. The form POSTs to /admin/login. If the password matches the ADMIN_PASSWORD Replit secret, set a secure httpOnly session cookie and redirect to /admin/dashboard. Move the entire admin dashboard to /admin/dashboard. Add /admin/logout that clears the cookie and redirects to /admin login. Install express-session if not already present.
5. CUSTOM ADMIN URL: Change the admin login page route from /admin to /burkmin. Update all internal admin links and redirects to use /burkmin, /burkmin/dashboard, and /burkmin/logout. The old /admin route should return 404.
6. ROBOTS.TXT: Add Disallow: /burkmin to robots.txt so search engines never index it.
7. ADMIN PASSWORD: Read the password from the ADMIN_PASSWORD Replit secret. Prompt me to set this secret to a value of my choice — do not generate a random one.